ClassicalRnTestError produces the training curve data. It averages over random sets of chunks.

RandTestSize does the same, but with the quantum model of reservoir.

Other files are either data generated and saved by these codes, or auxillary functions.